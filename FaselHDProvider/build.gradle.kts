// =================================================================
// النسخة النهائية - بناءً على المشروع المرجعي extensions
// =================================================================
version = 1 // رقم الإصدار

cloudstream {
    // يمكنك إضافة أي معلومات إضافية هنا لاحقًا
    description = "FaselHD Provider"
    authors = listOf("YourName")
    status = 1 // 1 = يعمل
    tvTypes = listOf("Movie", "TvSeries", "Anime")
}
